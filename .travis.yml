dist: trusty

language: java

jdk:
- oraclejdk8

cache:
  directories:
  - "$HOME/.m2"
  - "./client/node_modules/"
  - "$HOME/.sonar"
  - "$HOME/.sonarscanner"


before_script:
- npm install --prefix client/

# Sonar cloud addon
addons:
  sonarcloud:
    organization: final_project_text_messenger
    token: 061e53bf3c59953e76740f290eab418b35d20486

script:
- mvn clean test jacoco:report coveralls:report
- npm run build --prefix client/
- sonar-scanner